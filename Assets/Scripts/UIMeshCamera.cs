using UnityEngine;

public class UIMeshCamera : MonoBehaviour
{
    // Tiny script, just to hold references to the camera and mesh renderer of the sphere
    public Camera uiCamera;
    public MeshRenderer meshRenderer;
}
